# Changelog images

Add images here for changelog entries (e.g. `film-tv-search.png`). In `changelog.json` set `"image_url": "/changelog/film-tv-search.png"` for the entry. Leave `image_url` as `null` if you don't have an image.
